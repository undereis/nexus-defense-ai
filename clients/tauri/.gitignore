# dependências e builds do cliente Tauri (não versionar)
node_modules
dist
dist-ssr
*.local

# Rust / Tauri
src-tauri/target
src-tauri/gen

# ícones versionados (placeholder gerado via `npm run tauri icon`; troque pelo
# logo real quando tiver). Mantidos no git para o app buildar out-of-the-box.
